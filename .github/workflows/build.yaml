name: build

on: [push]

jobs:
  build:
    name: Build our App
    runs-on: ubuntu-latest
    steps:
      - name: Pull Code
        uses: actions/checkout@v3
      - name: Print current directory
        run: pwd
      - name: List of file
        run: ls